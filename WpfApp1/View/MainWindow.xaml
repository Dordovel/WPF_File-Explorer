<Window x:Class="WpfApp1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1"
        mc:Ignorable="d"
        Title="MainWindow" Height="460" Width="810"
        ResizeMode="NoResize">
    <Grid>
        <ListView Foreground="Brown" HorizontalAlignment="Left" Height="392" VerticalAlignment="Top" Width="813" x:Name="file_list" Margin="-5,39,-4,0">
            <ListView.View>
                <GridView ColumnHeaderContainerStyle="{StaticResource myHeaderStyle}">
                    <GridViewColumn Width = "{Binding ElementName = file_list, Path = ActualWidth}"
                        DisplayMemberBinding="{Binding Path=Title}">
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
        <Button HorizontalAlignment="Right" VerticalAlignment="Top" Height="40" Margin="0,0,754,0" Width="40">
            <StackPanel Width="40" Margin="-20, 0,-10,-10">
                <Image Source="../Image/Back.png" Height="20" Width="20"/>
            </StackPanel>
        </Button>
    </Grid>
</Window>
